rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write for DayToDay application sync
      allow read, write: if true;
    }
  }
}
